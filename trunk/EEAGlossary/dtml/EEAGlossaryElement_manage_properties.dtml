<dtml-var manage_page_header>
<dtml-var manage_tabs>
<dtml-if "'Manager' in _.SecurityGetUser().getRoles() or 'GlossaryManager' in _.SecurityGetUser().getRoles()">
<dtml-call "RESPONSE.redirect('propertysheets/Basic/manage')">
<dtml-else><p class="form-help">
<b>Sorry, access denied.</b><br><br>
These properties can be changed only by a user with a <b>Manager</b> role.<br><br>
<dtml-var help>
</p></dtml-if>
<dtml-var manage_page_footer>
